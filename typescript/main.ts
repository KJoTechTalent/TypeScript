function myName(naame: string, surname: string){
    let complete = naame + " " + surname;
    console.log(complete);
}
let naame = "Kelly";
let surname = "Almeida";
myName(naame,surname);

// ej 2
 
function calculator(num1: number, num2: number){
    let plus = num1 + num2;
    let multiply = num1 * num2;
    let divide = num1/num2;
    console.log("suma= " + plus);
    console.log("multiplicación= " + multiply);
    console.log("división= " + divide)
}
let num1 = 34;
let num2 = 15;
calculator(num1,num2);

// ej 3

function allMyNames(first: string, second: string ){
    let print = allMyNames;
    alert(allMyNames);
}

